#include "holberton.h"
#include <stdio.h>

/**
 * mul - operation into two numbers
 *
 * @a: contain a number
 * @b: contain a number
 *
 * Return: math operation.
 */

int mul(int a, int b)
{

	return (a*b);
}
