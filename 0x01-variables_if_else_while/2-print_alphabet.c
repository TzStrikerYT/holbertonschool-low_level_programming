#include <stdio.h>
/**
* main - generate random number and show if option
*
* Return: Always 0 (Success)
*/
int main(void)
{
int letra;
for (letra = 97; letra < 123; letra++)
{
putchar(letra);
}
putchar('\n');
return (0);
}
