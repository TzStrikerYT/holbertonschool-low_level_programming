#include "holberton.h"

/**
 * set_string - replace second string to the first string.
 *
 * @s: is a poiter to pointer.
 * @to: is pointer only :).
 * Return: void variable
 */

void set_string(char **s, char *to)
{
	*s = to;
}
