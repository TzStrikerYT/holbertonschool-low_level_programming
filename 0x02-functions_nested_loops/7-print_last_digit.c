#include "holberton.h"
/**
 * print_last_digit print the last digit
 *
 * c is a in variable
 *
 * Return: 0 if the letter islower.
 */

int print_last_digit(int n)
{
        int res = n % 10;
        return (res);
}
